[project]
name = "graph"
version = "0.1.0"
description = ""
authors = [
    {name = "mkirate"}
]
requires-python = ">=3.10"
dependencies = [
    "pyspark>=3.5.0,<4.0.0"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
